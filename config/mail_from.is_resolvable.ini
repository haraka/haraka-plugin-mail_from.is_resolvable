[main]
allow_mx_ip=false
re_bogus_ip=^(?:0\.0\.0\.0|255\.255\.255\.255|127\.)
; Maximum time (ms) to wait on each DNS call (MX lookup + MX-host resolution).
; A timeout returns DENYSOFT so the sender retries. Default: 5000.
;timeout_ms=5000

[reject]
no_mx=deny
